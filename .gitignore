node_modules/
screenshots/
offsets.json
*.log
*.png
.DS_Store
Thumbs.db

# Claude Code HUD state (로컬 경로/session id 포함 — 공개 레포에 들어가면 안 됨)
.omc/

# 내부 설계 스펙 (공개 불필요)
WILSON-SPEC.md
docs/

# 토큰 사용량 집계 캐시 (로컬 전용) — .gitkeep만 유지, 그 외 모든 파일 무시
cache/*
!cache/.gitkeep

# 로컬 아카이브 (구버전 파일 보관소)
_archive/
